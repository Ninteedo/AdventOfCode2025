package utility

class DayTemplate extends IDay(0) {
  override def execute(input: String): Result = {
    Result(part1(), part2())
  }

  private def part1() = {
    incomplete
  }

  private def part2() = {
    incomplete
  }
}
